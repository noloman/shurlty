<div class="hero min-h-screen bg-base-200">
  <div class="flex-col justify-center hero-content lg:flex-row">
    <div class="text-center lg:text-left">
      <h1 class="mb-5 text-5xl font-bold">
        shURLty
      </h1>
      <p class="mb-5">shURLty
 is a free tool to shorten URLs and generate short links</p>
      <p class="mb-5">
        shURLty
 allows to create a shortened link making it easy to share
      </p>
    </div>
    <div class="card flex-shrink-0 w-full max-w-sm shadow-2xl bg-base-100">
      <div class="card-body">
        <div class="card-body">
          <%= form_with url: new_link_path, html: {class: 'form-control' } do |form| %>
            <%= form.text_field :url, placeholder: 'Enter URL here' %>
            <%= form.submit 'Make it short!', class: 'form-control mt-6 btn btn-primary' %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
